name: Discord Test

on:
  workflow_dispatch:

jobs:
  send-message:
    runs-on: ubuntu-latest

    steps:
      - name: Send Discord Message
        run: |
          curl -H "Content-Type: application/json" \
          -d "{\"content\":\"🚀 GitHub Action is working!\"}" \
          ${{ secrets.DISCORD_WEBHOOK }}
